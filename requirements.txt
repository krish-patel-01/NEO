# Direct dependencies only. `pip install -e .` reads pyproject.toml and is
# preferred; this file is kept for the plain `pip install -r` path.
groq>=0.10.0
python-dotenv>=1.0.0
requests>=2.32.0
urllib3>=2.2.0
wolframalpha>=5.1.0
